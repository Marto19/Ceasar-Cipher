#include <iostream>

//The following function recieves the cyphered message and returns it decrypted
std::string decrypt(std::string message, int shift){
    for(int i =0; i < message.length(); ++i){

    }
}

int main()
{
    std::cout << "Enter the crypted message message: ";
    std::string message;
    std::cin >> message;
    std::cout << "Your decrypted message is: " << std::endl;
}
